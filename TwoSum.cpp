/*
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        vector<int> sum(2, 0);
        for (auto i = 0; i != (nums.size() - 1); ++i)
            for (auto j = i + 1; j != nums.size(); ++j)
                if (nums[i] + nums[j] == target)
                    sum = {i, j};
        return sum;
    }
};
*/

using ash solution is (O)
